#
# Application properties will be set here
#
application-properties:
  # One single point for reusability
  message-topic:
    new-comment:
      topic-name: new-comment
      type: NEW_COMMENT
    new-post:
      topic-name: new-post
      type: NEW_POST
    new-subscriber:
      topic-name: new-subscriber
      type: NEW_SUBSCRIBER
    new-post-following:
      topic-name: new-post-following
      type: NEW_POST_FOLLOWING
  security:
    # JWT issuer URI of our custom auth server (see docs/SPRING_BASE_INTEGRATION.md).
    # Used only for `iss` claim validation — we deliberately do NOT bootstrap from
    # /.well-known/openid-configuration so the app can start while the auth server
    # is unreachable. Token decoding will then fail at request time with 401.
    issuer-uri: ${JWT_ISSUER_URI:http://localhost:8080}
    jwk-set-uri: ${JWT_JWK_SET_URI:http://localhost:8080/.well-known/jwks.json}
    # Expected `aud` claim on incoming access tokens. Must match the client_id registered
    # for this service on the auth server.
    client-name: ${JWT_CLIENT_NAME:spring-base}
    no-authenticated-verb-urls:
      - { method: GET, url: /post/** }
      - { method: GET, url: /category/** }
      - { method: GET, url: /comment/** }
    no-authenticated-urls:
      - /free/**
      - /health
      - /health/**
      - /v3/api-docs/**
      - /swagger-ui.html
      - /swagger-ui/**
      - /swagger-resources/**
      - /configuration/ui/**
      - /configuration/security/**
      - /webjars/**
    high-privilege-verb-urls:
      - { method: DELETE, url: /category/** }
      - { method: POST, url: /category/** }
#
# End of application properties
#
server:
  port: ${SERVER_PORT:8088}
  shutdown: GRACEFUL
  servlet.context-path: /api
spring:
  application.name: spring-base
  cloud:
    function.definition: keyInvalidated
    stream:
      bindings:
        newPost-out-0:
          destination: ${application-properties.message-topic.new-post.topic-name}
        newSubscriber-out-0:
          destination: ${application-properties.message-topic.new-subscriber.topic-name}
        newComment-out-0:
          destination: ${application-properties.message-topic.new-comment.topic-name}
        newPostFollowing-out-0:
          destination: ${application-properties.message-topic.new-post-following.topic-name}
        # Inbound: JWKS key-rotation events from the auth server (see SPRING_BASE_INTEGRATION.md §7).
        # Body is a no-op log line; lazy-on-unknown-kid is the correctness backstop.
        keyInvalidated-in-0:
          destination: key-invalidated
          group: ${spring.application.name}
      default-binder: rabbit
  datasource:
    url: jdbc:postgresql://${POSTGRESQL_HOST:localhost}:${POSTGRESQL_PORT:5432}/${POSTGRESQL_NAME:spring-base}
    username: ${POSTGRESQL_USERNAME:postgres}
    password: ${POSTGRESQL_PASSWORD:123456}
  # Make your default spring boot logging colorful (if you are using Community version lmao)
  output.ansi.enabled: ALWAYS
  threads.virtual.enabled: true # Make use of Virtual Threads if possible
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:rabbitmq}
    password: ${RABBITMQ_PASSWORD:123456}
  security.oauth2.resourceserver.jwt:
    jwk-set-uri: ${application-properties.security.jwk-set-uri}
decorator.datasource.p6spy:
  custom-appender-class: com.vulinh.utils.MyP6SpyLogging
  logging: CUSTOM
management.endpoints.web:
  base-path: ''
  exposure.include: health
springdoc.swagger-ui:
  path: /swagger-ui
  disable-swagger-default-url: true
